# Cloudflare Pages headers for docs.gastownhall.ai
# Order matters: more specific rules should come AFTER general rules

# Default for all paths - no cache (catches HTML pages at /foo/, /bar/, etc.)
/*
  Cache-Control: no-cache

# Hashed assets (CSS/JS) - immutable, cache forever (overrides default)
/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# Images - cache for a day (overrides default)
/images/*
  Cache-Control: public, max-age=86400

# Search index - short cache with revalidation (overrides default)
/pagefind/*
  Cache-Control: public, max-age=3600, must-revalidate
